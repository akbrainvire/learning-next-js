//This is the root route for '/blog'
//To create the nested route we have to create the folder with the name of the route that we want to create and for the nested route in that component
// we can create the file for that route

import React from "react";

const blog = () => {
  return <div>This is Blog page</div>;
};

export default blog;
